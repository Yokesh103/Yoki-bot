# YokiBot — Clean Full Rebuild


This repo contains a clean rebuild of your trading microservice suite. Start with the live-feed microservice.


## Quick start (local using docker-compose)
1. Copy your Angel One credentials into `live-feed-microservice/app/.env`.
2. From repo root run:
```bash
docker-compose up --build